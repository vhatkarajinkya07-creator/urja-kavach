# Project Rules & Preferences

- **Automatic Git Commits**: After completing any code edit or feature implementation step, automatically stage (`git add .`) and commit (`git commit -m "..."`) the changes without asking for confirmation.
